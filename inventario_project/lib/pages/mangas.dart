import 'package:flutter/material.dart';

class Mangas extends StatelessWidget {
  const Mangas({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
     
    );
  }
}