import 'package:flutter/material.dart';

class Tanques extends StatelessWidget {
  const Tanques({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(""),
    );
  }
}